#include <iostream>
#include <fstream>
#include <vector>
#include <math.h>
using namespace std;
typedef pair<int,int> ipair;
char grid[1000][1000];
int fire_grid[1000][1000];
void firebfs()
int main(){
    int t; 
    while(cin >> t){
        int a,b; 
        cin >> a >> b;
        for(int i = 0; i < a; i ++){
            for(int j = 0; j < b; j++){
                cin >> grid[i][j];
            }
        }

    }
	return 0;
}
