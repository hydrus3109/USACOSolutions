#include <iostream>
#include <fstream>
#include <algorithm>
#include <vector>
#include <queue>
#include <string>
using namespace std;
int lines, words, syllabes;
vector<char> rhyme;
vector<pair<int,int>> list;
int store[101][101];

int main(){
    cin >> words >> lines >> syllables
    for(int i =0; i <  words; i++){
        int a,b;
        cin >> a >> b;
        lsit.push_back(make_pair(a,b));
    }
    for(int i = 0; i < size; i++){
        char a;
        cin >> a;
        rhyme.push_back(a);
    }

    return 0;
}