#include <iostream>
#include <fstream>
#include <vector>
#include <math.h>
using namespace std;
typedef pair<int,int> ipair;
vector<ipair> coords;
double dist(ipair x, ipair y){

}
int main(){
    int N, K;
    cin >> N >> K;
    for(int i = 0; i < N; i ++){
        int a,b; 
        cin >> a >> b;
        coords.push_back({a,b});
    }
    for(int i = 0; i < N; i++){
        for(int i = 0;  < N; i++ ){

        }
    }
	return 0;
}
